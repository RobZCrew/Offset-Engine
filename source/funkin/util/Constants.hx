package funkin.util;

class Constants {
    // GAME DEFAULTS
    /// Default difficulty for charts
    public static var DEFAULT_DIFICULTY = 'normal';

    // Default list of difficulties for charts
    public static var DEFAULT_DIFFICULTY_LIST = ['easy', 'normal', 'hard'];

    // Default combo y position
    public static var DEFAULT_COMBO_Y = 100;
}